디스코드 계정으로 로그인하면 가입한 서버들의 대화가 **하나의 타임라인**으로 모입니다.
채널 트리를 몰라도 커뮤니티에 참여할 수 있게 만드는 것이 목적입니다.

## 받아서 바로 쓰기

| 파일 | |
|---|---|
| **Fuse Setup 0.1.0.exe** | 설치본. 시작 메뉴와 바탕화면 바로가기가 생깁니다 |
| **Fuse-0.1.0-portable.exe** | 무설치. 그냥 실행하면 됩니다 |

서버가 앱 안에서 돌기 때문에 **호스팅이 필요 없습니다.**
처음 실행하면 디스코드 값 세 개를 넣는 설정 화면이 뜨고, 그 값은 이 컴퓨터에만 저장됩니다.

넣기 전에 개발자 포털에서 두 가지를 해두세요. 설정 화면에도 같은 안내가 있습니다.

1. **Bot → Privileged Gateway Intents** 에서 `MESSAGE CONTENT` 와 `SERVER MEMBERS` 켜기
2. **OAuth2 → Redirects** 에 `http://localhost:5195/auth/callback` 추가

> 서명하지 않은 실행 파일이라 Windows SmartScreen 이 한 번 경고합니다.
> **추가 정보 → 실행**을 누르면 됩니다.

## 무엇이 되나

- 가입한 서버들의 글이 한 피드로. 서버별 필터는 위에 고정
- 글쓰기·답글·인용·수정·삭제. 사진·동영상·파일 첨부
- 글은 **회원님의 이름과 프로필 사진**으로 디스코드에 올라갑니다
- 답글은 디스코드 **네이티브 답장**으로 달립니다
- 좋아요, 이모지 리액션, `@` 멘션 자동완성, 디스코드 마크다운 전체
- 실시간 반영 — 새 글, 리액션, 수정·삭제, 입력 중 표시
- 멘션·답글·좋아요·쪽지 알림
- 쪽지 (사진·파일 첨부 가능, 상대의 디스코드 DM 으로도 전달)
- 검색, 서버 둘러보기 및 앱 안에서 가입
- 당겨서 새로고침 — 로고의 도화선이 타들어갑니다

## 보이는 범위

**회원님이 디스코드에서 볼 수 없는 채널은 Fuse 에도 나오지 않습니다.**
요청마다 역할과 채널 권한 오버라이트를 그대로 계산하고, 실시간 이벤트도 같은 검사를 통과해야 전달됩니다.

계정 비밀번호나 유저 토큰은 쓰지 않습니다. 공식 OAuth2 와 봇 토큰만 씁니다.

## 아직 안 되는 것

디스코드 API 가 서드파티 앱에 열어주지 않는 영역입니다.

- 기존 DM 과 그룹 DM 읽기
- 친구 목록
- 음성·화면공유

쪽지 기능은 이 한계 안에서 만든 Fuse 자체 대화입니다.

---

[설치와 봇 연동](SETUP.md) · [데스크톱 앱](DESKTOP.md) · [서버에 올리기](DEPLOY.md) · [전체 변경 이력](CHANGELOG.md)
