# 데스크톱 앱으로 쓰기

서버를 앱 안에서 돌립니다. 그래서 **호스팅이 필요 없습니다.**
디스코드 게이트웨이 연결도, 웹소켓도, 파일 저장도 전부 평소처럼 동작합니다.

```
Fuse.exe
  ├─ Express 서버 (localhost)
  ├─ 디스코드 봇 (게이트웨이 연결 유지)
  └─ 앱 창 (화면)
```

---

## 빌드하기

```bash
npm run build:app
```

`dist/` 에 두 가지가 나옵니다.

| 파일 | 설명 |
|---|---|
| `Fuse Setup 0.1.0.exe` | 설치본. 시작 메뉴와 바탕화면 바로가기가 생깁니다 |
| `Fuse-0.1.0-portable.exe` | 설치 없이 그냥 실행 |

아이콘만 다시 만들려면 `npm run build:appicon`.

---

## 처음 실행하면

자격증명이 없으면 설정 화면이 먼저 뜹니다. 값 세 개를 넣으면 됩니다.

| 넣을 것 | 어디서 |
|---|---|
| 클라이언트 ID | 개발자 포털 → OAuth2 |
| 클라이언트 시크릿 | 개발자 포털 → OAuth2 → Reset Secret |
| 봇 토큰 | 개발자 포털 → Bot → Reset Token |

넣기 전에 두 가지를 먼저 해두세요. 설정 화면에도 같은 안내가 있습니다.

1. **Bot → Privileged Gateway Intents** 에서 `MESSAGE CONTENT` 와 `SERVER MEMBERS` 켜기
2. **OAuth2 → Redirects** 에 아래 주소 추가

   ```
   http://localhost:5195/auth/callback
   ```

한 번 넣으면 다시 묻지 않습니다.

---

## 저장 위치

`%APPDATA%\Fuse\`

| | |
|---|---|
| `credentials.json` | 디스코드 자격증명 (이 컴퓨터에만) |
| `data/db.json` | 좋아요·알림·쪽지·설정 |

앱을 지워도 이 폴더는 남습니다. 완전히 초기화하려면 폴더째 지우세요.
자격증명을 바꾸고 싶을 때도 `credentials.json` 만 지우면 설정 화면이 다시 뜹니다.

---

## 휴대폰에서도 보려면

같은 Wi-Fi 라면 PC 에서 도는 Fuse 를 휴대폰 브라우저로 열 수 있습니다.

1. PC 의 내부 IP 를 확인합니다 (`ipconfig` → IPv4 주소)
2. 휴대폰에서 `http://<PC주소>:5195` 로 접속
3. 브라우저 메뉴에서 **홈 화면에 추가** — 아이콘이 깔리고 전체화면으로 뜹니다

이때 디스코드 개발자 포털 Redirects 에 그 주소도 추가해야 로그인이 됩니다.

```
http://192.168.0.10:5195/auth/callback
```

> **apk 는 왜 안 만드나요?**
> 안드로이드에서 Node 서버를 돌리는 건 현실적이지 않아서, apk 를 만들어도
> 결국 어딘가 떠 있는 서버를 가리키는 웹뷰 껍데기가 됩니다.
> 위의 홈 화면 추가가 같은 결과를 훨씬 간단하게 냅니다.

---

## 잘 안 될 때

| 증상 | 원인 |
|---|---|
| 창이 뜨는데 계속 설정 화면 | 봇 토큰이 틀렸거나 특권 인텐트가 꺼져 있음 |
| "서버가 응답하지 않습니다" | 위와 같음. `%APPDATA%\Fuse\credentials.json` 을 지우고 다시 입력 |
| 로그인 눌렀는데 `invalid_redirect_uri` | Redirects 에 `http://localhost:5195/auth/callback` 이 없음 |
| 피드가 비어 있음 | 봇을 초대한 서버에 내 계정도 들어가 있어야 합니다 |
| 앱이 두 번 안 켜짐 | 정상입니다. 게이트웨이가 중복으로 붙지 않게 한 번만 실행됩니다 |

포트를 바꾸려면 `FUSE_PORT` 환경변수를 주고 실행하세요. Redirects 도 같이 바꿔야 합니다.
