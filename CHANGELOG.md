# 변경 이력

이 파일은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 씁니다.

버전마다 `v0.1.0` 같은 깃 태그가 붙어 있어서 아무 때나 그 시점으로 되돌릴 수 있습니다.

```bash
npm run release:list
```

---

## [미출시]

### 추가

- **데스크톱 앱(exe)** — 서버를 앱 안에서 돌려 호스팅이 필요 없습니다.
  게이트웨이 연결·웹소켓·파일 저장이 전부 평소처럼 동작합니다.
  첫 실행 때 디스코드 자격증명을 받는 설정 화면이 뜨고, 값은 `%APPDATA%\Fuse` 에만 저장됩니다.
  `npm run build:app` 으로 설치본과 무설치본이 나옵니다. ([DESKTOP.md](DESKTOP.md))
- **답장 맥락 줄** — 답장에 원본 작성자·내용 미리보기가 붙고 누르면 원본으로 이동합니다
- 채널 화면에서는 답글도 시간순 그대로 보입니다

### 변경

- **답글이 디스코드 네이티브 답장으로** 바뀌었습니다 (전에는 스레드).
  `FUSE_REPLY_MODE` 로 `reply`(기본) / `webhook` / `thread` 중 고를 수 있습니다.
  웹훅 API 에는 `message_reference` 가 없어서, 네이티브 답장과 "내 이름으로 보이기" 는
  동시에 되지 않습니다. `reply` 모드에서 디스코드 쪽 작성자는 봇 이름이 됩니다.
- **쪽지에 사진·파일 첨부**가 됩니다. 디스코드로 전달되는 알림도 임베드 카드가 아니라 본문만 갑니다.
- `.env` 덮어쓰기를 자격증명 세 개로 좁혔습니다. 이제 `PORT=8080 npm start` 가 정상 동작합니다.

### 고침

- 공통 조각을 `ui.js` 로 분리하면서 import 를 빠뜨려 **설정 화면이 열리지 않던 문제**.
  화면 하나가 터져도 이전 화면이 남지 않도록 라우터에 예외 처리를 넣었습니다.

---

## [0.1.0] - 2026-08-23

첫 공개 버전.

### 추가

- **통합 타임라인** — 가입한 서버들의 대화가 하나의 피드로. 서버별 필터 칩(상단 고정)
- **디스코드 OAuth2 로그인** — 비밀번호나 유저 토큰을 쓰지 않음
- **권한 계산** — 회원님이 디스코드에서 볼 수 없는 채널은 Fuse에도 나오지 않음.
  역할과 채널 오버라이트를 요청마다 그대로 계산하고 실시간 이벤트도 같은 검사를 통과해야 전달
- **글쓰기** — 웹훅으로 회원님의 이름·프로필 사진을 달고 게시
- **답글** — 디스코드 네이티브 답장 (`FUSE_REPLY_MODE` 로 웹훅/스레드 방식 선택 가능)
- **인용·수정·삭제**, 사진·동영상·파일 첨부(최대 4개, 8MB), 스포일러, 라이트박스
- **좋아요**(Fuse 자체)와 **이모지 리액션**(디스코드에 실제 반영)
- **@멘션 자동완성** — 입력창에는 `@이름`, 전송 시 디스코드 문법으로 변환
- **디스코드 마크다운** 전부 — 굵게·기울임·밑줄·취소선·스포일러·코드블록·인용·목록·제목·타임스탬프
- **실시간** — 새 글, 좋아요, 리액션, 수정·삭제, 입력 중 표시
- **알림** — 멘션·답글·좋아요·쪽지. 앱이 켜져 있으면 화면 카드, 아니면 브라우저 알림. 탭 제목에 안 읽은 수
- **쪽지** — Fuse 안 1:1 대화. 사진·파일 첨부 지원. 상대의 디스코드 DM으로도 전달되고 거기 답장하면 되돌아옴
- **검색** — 글·사람·서버·채널 통합. 검색어가 비면 최근 본 항목
- **서버 둘러보기** — 봇이 있는 서버에 앱 안에서 바로 가입
- **당겨서 새로고침** — 로고의 도화선이 타들어가는 표시
- **데모 모드** — 봇 토큰 없이 예시 데이터로 전체 기능 실행

### 디자인

- 스레드(Threads) 레이아웃. 테두리를 쓰지 않고 배경 명도 차이로만 면을 나눔
- 아이콘은 Lucide 스트로크를 인라인 SVG로. morphicons 로 아이콘끼리 모양이 흘러가며 전환
- 이모지는 토스페이스, 본문은 Pretendard (둘 다 눈누/CDN)
- 모바일은 애플식 떠 있는 유리 탭바, 활성 표시도 색이 아닌 유리 재질
- 글을 누르면 그 카드가 목록 자리에서 상세 화면 맨 위로 올라가는 전환
- 라이트·다크·시스템 테마

[0.1.0]: https://github.com/a2den1/fuse-/releases/tag/v0.1.0
